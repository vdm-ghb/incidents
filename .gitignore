# OS
.DS_Store

# Dependencies
node_modules/

# Python bytecode cache
__pycache__/

# Internal reference material — not for version control
background files/
working documentation/*
scripts/

# Bow-Tie Explorer / Causal Theme Map — generated analysis views, published
# alongside the public site (linked from index.html), plus the protocol
# doc that explains their candidate/unverified evidence status
!working documentation/bowtie_view.html
!working documentation/causal_analysis.html
!working documentation/bowtie_full.json
!working documentation/causal_tags_full.json
!working documentation/ANALYSIS_CODING_PROTOCOL.md
NEW_INCIDENTS_DATABASE_ENTRIES.js
CLAUDE.md

# Raw IBTrACS source data (331MB+, exceeds GitHub's file-size limit) —
# data/storm_tracks.js is the curated subset actually loaded by the site
data/ibtracs/

# Raw PDF-figure extraction dumps (intermediate artifacts, not the
# selected/published images referenced from data/incidents.js)
images/seacor_fig17_extract/
images/skandi_figure12_extract/

# Playwright
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# Local Claude Code settings (machine-specific, not shared)
.claude/settings.local.json

# Local VS Code settings (machine-specific, not shared)
.vscode/

# Browser save-as copy of shared artifact reports (not a source file)
Metocean Incident Database — Fact-Check Audit.html
Metocean Incident Database — Fact-Check Audit_files/
